package com.example.jobqueue.dist_job_processor.model;

/**
 * Defines supported job categories handled by workers.
 * Each type represents a specific task logic.
 */
public enum JobType {

    EMAIL_SEND,
    IMAGE_RESIZE
}
